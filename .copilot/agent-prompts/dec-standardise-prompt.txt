You are working autonomously on the Enigma-NG project at C:\_DATA_\Repositories\github\Enigma-NG

OVERVIEW
========
You must make three sets of changes, then commit them:
  TASK 1 — Reformat all DEC-001 through DEC-024 entries in design/Design_Log.md
  TASK 2 — Add new DEC-025 entry to design/Design_Log.md
  TASK 3 — Update design/Software/Linux_OS/Power_Management.md

Read design/Design_Log.md in full before starting. Work through each DEC entry sequentially.


TASK 1 — STANDARDISE ALL DEC ENTRIES (DEC-001 through DEC-024)
================================================================

TARGET FORMAT FOR EVERY DEC ENTRY
-----------------------------------
Each entry must follow this structure exactly:

  ## DEC-NNN — [exact original title]

  - **Status:** [value]
  - **Date:** [value]
  - **Category:** Electrical
  - **Area:** [value]

  ### Decision

  [decision body]

  ### Rationale

  [rationale content]

  ### Impact

  [only if original had impact / files-updated content]

  ### Alternatives Considered

  [only if original had this section]

  ---

METADATA BLOCK RULES
---------------------
Status field:
  - DEC-001 to DEC-009 have no Status line. Add: Decided
  - DEC-008 is superseded; use: Superseded by DEC-012
  - DEC-010: Deferred — Accepted risk for prototype stage
  - DEC-011 onwards: preserve existing Status value exactly

Date field: preserve exactly as written in original.

Category field: ALL entries DEC-001 through DEC-024 get: Category: Electrical

Area field:
  - If original has Area: use it
  - If original has Affects: use that content
  - DEC-001: Power Module, Controller Board, Link-Alpha connector
  - DEC-002: Power Module PoE subsystem, T2 transformer, TPS23730 feedback network
  - DEC-003: Power Module PoE output, OR-ing diode (LM74700-Q1), eFuse input
  - DEC-004: LTC3350 supercap charger, PoE power budget
  - DEC-005: Power Module eFuse (U1)
  - DEC-006: Power Module USB-C PD handshake (STUSB4500), USB-C adapter requirement
  - DEC-007: Power Module 5V Buck (U2A/U2B)
  - DEC-008: CM5 USB-C PD emulator (TPS25750)
  - DEC-009: Controller Board Diagnostic Bank-Alpha connector
  - DEC-010: Controller Board Diagnostic Bank-Alpha / Bank-Beta connectors
  - DEC-011: Power Module islands, Controller Board routing, rotor stack power source
  - DEC-012: Power Module U4, schematic, PCB footprint
  - DEC-013: Power Module L3, PCB footprint
  - DEC-014 onwards: use their existing Affects/Area content
  - DEC-020 also has: References: QUE-001, Certification_Evidence.md §2.2 — keep as extra metadata line
  - DEC-021 also has: References: DR-PM-07, DR-PM-09, DEC-020 — keep as extra metadata line
  - DEC-022: Area: JTAG Daughterboard — Clock Source (FT232H)

SECTION HEADING RULES
----------------------
- "Decision" content (was bold inline **Decision:** ) -> ### Decision
- "Rationale" content -> ### Rationale
- "Impact", "Files Updated", "Impact on X" -> ### Impact
- "Alternatives Considered" -> ### Alternatives Considered
- Unique sections like "Post-Prototype Action Required", "Architectural Rule", "Poka-Yoke Safety Note",
  "OVLO Constraint", "Key Parameters", "Constraints", "Cross-ref" -> preserve as ### subsection with original name
- DEC-008 blockquote warning about supersession: keep it right after the ## heading line before metadata block

PRESERVATION RULES
------------------
- Do NOT drop any text, tables, code blocks, blockquotes, notes, or cross-references
- Preserve all table content exactly
- Preserve all blockquotes (> lines) exactly
- Preserve all numbered/bulleted lists exactly
- DEC-016 has a complex table and update notes — preserve everything exactly


TASK 2 — ADD DEC-025
=====================

Insert this new entry after DEC-024 (after its --- separator) and before the "## Open Questions" section:

## DEC-025 — CM5 Shutdown Mechanism: Interrupt-Driven via Custom Linux Driver (Deferred)

- **Status:** Deferred — Software PoC Stage
- **Date:** 2026-04-09
- **Category:** Software
- **Area:** Software / Linux OS — CM5 Power Management Shutdown Path

### Decision

The final implementation of the CM5 graceful shutdown mechanism — triggered by the LTC3350 BACKUP
signal — is deferred to the **Software PoC stage**, pending hardware availability for integration
testing. The real implementation will use a **custom Linux kernel driver** that registers the BACKUP
signal as a hardware interrupt, rather than a userspace polling daemon.

The PWR_GD GPIO (GPIO 27, MCP121T-450E) remains the hard-backstop interrupt and requires no driver
development — it is handled by the standard gpio-shutdown device tree overlay.

### Rationale

- The custom driver approach eliminates polling latency entirely (interrupt response is effectively
  immediate vs. up to 500 ms for a 2 Hz poll cycle).
- Driver development requires the physical hardware to be available for testing and validation.
  Writing and verifying kernel interrupt handlers against simulated hardware is impractical.
- The system is designed for operational sessions of 15–30 minutes or longer. The hold-up window
  (>=21.7 s) is a generous safety margin; an unplanned shutdown from a fully-charged state is expected
  to be harmless. Deferring the driver to the PoC stage does not create a risk for hardware bring-up.
- Placeholder pseudocode in design/Software/Linux_OS/Power_Management.md documents the intended
  behaviour for reference; it is not a specification.

### Impact

- design/Software/Linux_OS/Power_Management.md Phase 1: Polling daemon pseudocode replaced with
  deferred-decision note referencing DEC-025. The PWR_GD gpio-shutdown backstop (Phase 2) is
  unchanged and remains the active hardware protection path until the driver is written.

---


TASK 3 — UPDATE Power_Management.md
=====================================

File: design/Software/Linux_OS/Power_Management.md

STEP 3a — Fix Overview section bullets

Find and replace these two items in the Overview section:

OLD (exact text, find these two numbered bullets plus the paragraph after):
  1. **LTC3350 BACKUP trigger (I²C polling)** — primary early-warning mechanism; the BACKUP bit is set when 5V_MAIN falls below 4.40V
     (R14=26.7kΩ; see Power_Module/Design_Spec.md DR-PM-08), triggering a graceful shutdown with the full ≥21.7-second supercap hold-up window available.
  2. **PWR_GD (GPIO 27 interrupt)** — secondary hard-backstop from MCP121T-450E voltage supervisor; deasserts (goes LOW) when 5V_MAIN < 4.5V.
     Triggers an emergency sync-and-halt if the I²C daemon fails to catch the BACKUP event.

  The recommended approach is **Option C**: poll the LTC3350 via I²C for the BACKUP alert as the primary early-warning mechanism,
  with the PWR_GD GPIO as a hard backstop interrupt. This gives the CM5 the full ≥21.7-second hold-up window to perform a graceful shutdown.

NEW (replace with):
  1. **PWR_GD (GPIO 27)** — first hardware event; MCP121T-450E voltage supervisor deasserts (goes LOW) when 5V_MAIN < 4.5V (~10 ms after mains loss).
     Acts as primary interrupt trigger in the interim implementation (gpio-shutdown device tree overlay).
  2. **LTC3350 BACKUP signal** — fires when 5V_MAIN drops to 4.40V (R14=26.7kΩ; see Power_Module/Design_Spec.md DR-PM-08), engaging supercap hold-up.
     This is the preferred interrupt source for the final custom Linux driver implementation (see DEC-025). Provides the full >=21.7-second hold-up window for graceful shutdown.

  > **Implementation note:** The final interrupt-driven shutdown driver is deferred to the Software PoC stage (see **DEC-025**). Until then, the
  > PWR_GD gpio-shutdown overlay (Phase 2 below) is the active hardware protection path. The Phase 1 pseudocode is retained for reference only.

STEP 3b — Replace Phase 1 section

Find the entire Phase 1 section from its heading through the closing of the systemd unit file code block.
The heading is: ### Phase 1 — LTC3350 I²C Polling Daemon (Primary)
The section ends just before: ### Phase 2 — PWR_GD GPIO Hard Backstop (Backup)

Replace the entire Phase 1 section with:

### Phase 1 — LTC3350 BACKUP Interrupt Driver (Deferred — DEC-025)

> **Deferred to Software PoC Stage.** The final implementation uses a custom Linux kernel driver
> that registers the LTC3350 BACKUP signal as a hardware interrupt. This approach eliminates polling
> latency and is the preferred production mechanism. Development is deferred until the physical
> hardware is available for integration testing. See **DEC-025** in design/Design_Log.md.

**Intended behaviour (reference only):**

The driver will:
1. Register an interrupt handler on the LTC3350 ALERT/BACKUP pin.
2. On interrupt, read LTC3350 STATUS register (I2C 0x09, register 0x01) to confirm BACKUP bit (bit 3) is set.
3. Invoke kernel_power_off() or equivalent clean shutdown path.
4. Log the event to the kernel ring buffer for post-mortem analysis.

No polling loop, no userspace daemon, and no POLL_HZ parameter are required. The interrupt response
latency is determined by the kernel IRQ subsystem (typically < 1 ms), giving the full >=21.7-second
supercap hold-up window for OS shutdown.

**I2C parameters (for driver development reference):**

| Parameter | Value |
| --- | --- |
| I2C address | 0x09 |
| STATUS register | 0x01 |
| BACKUP bit | bit 3 (value 0x08) |
| ALERT pin | Active-low open-drain; connect to GPIO with pull-up |

STEP 3c — Fix Shutdown Timing Budget row

In the Shutdown Timing Budget table, find this row:
  | Daemon detects LTC3350 BACKUP asserted and initiates `systemctl poweroff` | <=510ms from power loss (<=500ms poll latency after BACKUP asserts at POLL_HZ=2) | OS begins graceful shutdown |

Replace with:
  | PWR_GD interrupt triggers gpio-shutdown (interim) / BACKUP interrupt triggers custom driver (deferred — DEC-025) | PWR_GD: ~10 ms from power loss; BACKUP: ~shortly after | OS begins graceful shutdown |


COMMIT
=======

After all edits, commit with:

  git -C C:\_DATA_\Repositories\github\Enigma-NG add design/Design_Log.md design/Software/Linux_OS/Power_Management.md
  git -C C:\_DATA_\Repositories\github\Enigma-NG commit -m "Standardise DEC format, add Category field, add DEC-025, update PM shutdown intent

  - Design_Log.md: DEC-001 through DEC-024 standardised to canonical format
  - Design_Log.md: Category: Electrical added to all existing DECs
  - Design_Log.md: DEC-025 added (CM5 Shutdown Mechanism deferred to Software PoC stage, Category: Software)
  - Power_Management.md: Overview corrected (PWR_GD fires first at 4.5V, BACKUP second at 4.40V)
  - Power_Management.md: Phase 1 polling daemon replaced with deferred-decision note + DEC-025 reference
  - Power_Management.md: Shutdown Timing Budget row updated to reflect interim/deferred paths

  Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"

Then report a summary of what was done, and flag any content that could not be cleanly migrated.
